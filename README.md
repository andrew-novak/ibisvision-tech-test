# ibisvision-tech-test
A project testing my ReactJS/Web Dev skills.

### To start in dev mode:
1. `npm i`
2. `npm run start`

### To build for production:
1. `npm run build`
2. Look for the compiled content in `build` directory.
